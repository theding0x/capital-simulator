// Package commodity will hold the domain types for the commodity-service:
// the Commodity itself, its dual character (use-value and value), and the
// labour-time computations that produce exchange-value. Empty for now;
// populated as Capital Ch. 1 is implemented.
package commodity
