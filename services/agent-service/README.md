# agent-service

Models the economic agents who populate the simulated economy: workers, capitalists, and (later) landowners. Agents are the bearers of class relations.

- Default port: `8082`
- Persistence: MongoDB

Run locally:

```bash
make run-agent-service
```
