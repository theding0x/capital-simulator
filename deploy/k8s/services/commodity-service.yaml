apiVersion: v1
kind: Service
metadata:
  name: commodity-service
  labels:
    app: commodity-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      name: http
  selector:
    app: commodity-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: commodity-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: commodity-service
  template:
    metadata:
      labels:
        app: commodity-service
    spec:
      containers:
        - name: commodity-service
          image: ghcr.io/theding0x/capital-simulator/commodity-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: SERVICE_ADDR
              value: ":8081"
            - name: LOG_LEVEL
              value: info
            - name: MYSQL_DSN
              value: "root:capital@tcp(mysql:3306)/commodity?parseTime=true&loc=UTC"
            - name: REDIS_ADDR
              value: redis:6379
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
