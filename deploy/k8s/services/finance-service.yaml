apiVersion: v1
kind: Service
metadata:
  name: finance-service
  labels:
    app: finance-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8085
      name: http
  selector:
    app: finance-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: finance-service
  template:
    metadata:
      labels:
        app: finance-service
    spec:
      containers:
        - name: finance-service
          image: ghcr.io/theding0x/capital-simulator/finance-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
              name: http
          env:
            - name: SERVICE_ADDR
              value: ":8085"
            - name: LOG_LEVEL
              value: info
            - name: MYSQL_DSN
              value: "root:capital@tcp(mysql:3306)/finance?parseTime=true&loc=UTC"
            - name: REDIS_ADDR
              value: redis:6379
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
